name: telegram_warehouse
version: "1.0"
config-version: 2

profile: telegram_warehouse

model-paths: ["models"]

models:
  telegram_warehouse:
    marts:
      materialized: table
    staging:
      materialized: view
